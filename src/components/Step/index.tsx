import React from "react";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Step Page",
  description: "This is Step Page for RemWaste",
  // other metadata
};

const StepPage = () => {
  return (
    <main>
		hi
    </main>
  );
};

export default StepPage;
